#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen - returns the length of the string
 * @s: takes in a character s
 *
 * Return: returns string length
 */
int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
