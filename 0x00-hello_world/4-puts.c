#include <stdio.h>

/**
 * main - where the code runs
 * Return: returns 0
 */
int main(void)
{
	/*initializing string*/

	char sentence[] = "\"Programming is like building a multilingual puzzle";

	puts(sentence);
	return (0);
}
